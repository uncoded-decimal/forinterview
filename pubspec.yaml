name: forinterview
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  firebase_auth: ^3.1.1
  firebase_core: ^1.6.0
  firebase_database: ^8.0.0
  flutter:
    sdk: flutter
  flutter_svg: ^0.22.0
  google_sign_in: ^5.1.1
  rxdart: ^0.27.2

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_rename_app: ^1.0.0
  flutter_test:
    sdk: flutter

# flutter pub get
# flutter pub run flutter_rename_app
flutter_rename_app:
  application_name: InterviewTask
  # dart_package_name: bank_app
  application_id: com.aditya.interviewtask
  # bundle_id: com.ios.bank
  # android_package_name: com.bank.app

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/vectors/
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
